/** Host loader entry for the MCP-roster-tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the MCP roster tab. */
export function apply(): void {}
